// Package api provides the unified registry for all Dash0 MCP tools.
package api

import (
	"github.com/npcomplete777/dash0-mcp/internal/registry"
)

// ToolProvider is an alias for registry.ToolProvider for external consumers.
type ToolProvider = registry.ToolProvider
